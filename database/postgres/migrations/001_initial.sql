-- ============================================================
-- groove-ai — PostgreSQL Migration: 001_initial
-- Applied by: psql or Alembic
-- ============================================================

-- This migration is captured by schema.sql for the initial setup.
-- Subsequent migrations should be added as 002_*.sql, etc.

-- Example future migration template:
-- ALTER TABLE repositories ADD COLUMN IF NOT EXISTS stars INTEGER DEFAULT 0;
